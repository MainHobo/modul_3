// See https://aka.ms/new-console-template for more information

using System;

namespace CSpiss // Note: actual namespace depends on the project name.
{
    internal class Program
    {
        
        static void Main(string[] args)
        {
            
            // Wikipedia - Intro til C#
            OppgaveKrokodillespillet.Run(args);
            
            // OppgaveLøkker.Run(args);
            // OppgaveIfElse.Run(args);
            // OppgaveMainMetodenArgs.Run(args);
            // OppgaveWriteReadLine.Run(args);
            // OppgaveVariabelordning.Run(args);
            // OppgaveReturn.Run(args);
        }
    }
}
